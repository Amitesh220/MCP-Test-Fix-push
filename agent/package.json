{
  "name": "ai-devops-agent",
  "version": "1.0.0",
  "description": "AI agent that orchestrates bug detection and code fixing via MCP server",
  "type": "module",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "test-only": "node agent.js --test-only",
    "fix-only": "node agent.js --fix-only"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "openai": "^4.73.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
